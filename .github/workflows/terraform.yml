name: Terraform Init & Plan

on:
  push:
    branches:
      - main

jobs:
  terraform:
    name: Terraform Init and Plan
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ AKIATOE2Y5EWVYNS47PD }}
      AWS_SECRET_ACCESS_KEY: ${{ st0OgC+z7RALMpI56l8pGasuKCaGRWaz05JuE+rVE }}    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.5.7

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan
